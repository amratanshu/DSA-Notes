A MST is a tree where 

N nodes
N - 1 edges
And all nodes are connected in a single Strongly connected component


There can be many spanning trees

The sum of the weights of all edges = if that is minimum - it is the munimum spanning tree

